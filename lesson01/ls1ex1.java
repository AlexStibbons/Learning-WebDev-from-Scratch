package folder.subfoler;

class ls1ex1 {
	public static void main(String[] args) {
		int weight = 500;
		String recipient = "Person A";
		String sender = "Person S";
		boolean express = true;
		System.out.println("Sender " + sender + " is sending a package to " + recipient + "." + "\n \t The weight of the package is " + weight + "grams. \n \t Express send: " + express + ".");
		
	}
}